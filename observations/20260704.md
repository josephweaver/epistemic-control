# 2026-07-04

## 012f2 Implementation Review

Completed slice:

- Added `WorkflowRunSubmission` and `SourceDocumentReference` to
  `internal/client`.
- Added `SubmitWorkflowRun`, `SubmitWorkflowRunFile`, and
  `LoadWorkflowRunSubmissionFile`.
- Kept inline workflow submission helpers as legacy compatibility methods.
- Updated `cmd/demo-client` to submit `demo-workflow-run.json`.
- Added `demo-workflow-run.json` with project/workflow source references.

Concepts introduced:

- The client now sends source references, not decoded workflow definitions.
- The client does not resolve refs to immutable commits; the controller/source
  resolver owns that.
- Legacy inline submission can coexist temporarily, but should not be the normal
  client/demo path.

Review questions:

1. Why should the client send repository/ref/path instead of reading and
   posting workflow JSON?
2. Which component should resolve `ref` to an immutable commit?
3. Why is `local:demo` treated as an opaque repository identity in the client?

Verification:

```text
go test ./internal/client ./cmd/demo-client
go test ./...
```

## Reconciled Session Notes

These notes were reconciled from a former secondary 2026-07-04 record after
`observations/` became the final location for dated records.

### Client file naming

Completed slice:

- Renamed `internal/client/workflow.go` to
  `internal/client/controller_client.go`.
- Renamed `internal/client/workflow_test.go` to
  `internal/client/controller_client_test.go`.
- Updated `internal/client/README.md` so the file map uses the new production
  filename.

Concept introduced:

- In Go, filenames do not define package membership or exported API. A file
  belongs to a package because of its `package` declaration.

Review questions:

1. Why did renaming `workflow.go` not require changing imports?
2. What does the `internal` directory mean for Go imports?
3. Why is `controller_client.go` more accurate than `workflow.go`?
4. What changes when an exported type is renamed?

### Controller client type rename

Completed slice:

- Renamed exported `WorkflowClient` type to `ControllerClient`.
- Renamed constructors from `NewWorkflowClient` and
  `NewWorkflowClientWithStarter` to `NewControllerClient` and
  `NewControllerClientWithStarter`.
- Updated internal client tests and the demo-client call site.

Concept introduced:

- Exported Go identifiers, meaning capitalized names, are visible to importing
  packages. Renaming an exported type or function requires updating every
  package that references that identifier.

Review questions:

1. Why did the constructor rename require a `cmd/demo-client` cleanup edit?
2. Why did methods like `SubmitWorkflowRun` keep their names?
3. What is the difference between package name `client` and type name
   `ControllerClient`?

### HCI schema for grouped feature planning

Completed slice:

- Added `hci-cadence/C(S)x(I)x.md`.
- Defined a cadence where the Strategic Concept is defined first, slices are defined
  one prompt at a time, and implementation proceeds feature by feature.
- Clarified that a feature implementation may span multiple prompts when the
  selected HCI specification requires it.
- Referenced `procedures/strategic-concept-design.md` for Strategic Concept writing and
  `procedures/operational-slice.md` for slice design.
- Added current-state and target-state framing for Strategic Concept design at strategic and
  operational levels.
- Updated `procedures/strategic-concept-design.md` so state framing is part of Strategic Concept writing.
- Added a file organization rule that favors splitting by logical concept.

Concept introduced:

- Prompt boundaries and feature boundaries are different. A slice can be
  one coherent unit while still being implemented across multiple budget-limited
  prompts.

Review questions:

1. Why should each slice be defined in its own prompt?
2. Why is a prompt budget not the same thing as a feature boundary?
3. What signals suggest that two structs should live in separate files?

### Feature-slice state and concept decisions

Completed slice:

- Updated `procedures/operational-slice.md`.
- Added `Current State`, `Target State`, and `Concept Decision` to the slice
  template.
- Clarified that slice current and target state may include bounded
  implementation details.
- Added guidance for deciding whether a slice adds a new concept or updates an
  existing one.
- Added guidance for when a new concept deserves its own file.

Concept introduced:

- Slice design is closer to implementation than Strategic Concept design. It should name the
  local code shape and expected artifacts without prescribing every line of
  code.

Review questions:

1. What implementation details belong in a slice's current state?
2. When should a new concept get its own file?
3. Why is adding a new slice not enough reason to add a new file?

### Implementation workflow

Completed slice:

- Added `procedures/implementation.md`.
- Referenced `hci-levels.md` as the source for selecting an HCI specification.
- Required the human to select an HCI specification before implementation code
  changes.
- Defined EC continuity across a chain of feature implementations.
- Documented multi-prompt feature implementation under one selected HCI
  specification.
- Added the Strategic Concept implementation command progression: create a concept branch,
  introduce each feature, wait for `next`, implement within budget, wait for
  `commit`, commit and push the slice, then introduce the next feature.
- Added the end-of-concept progression: state that no slices remain, offer final
  commit/push, pull request creation, merge to `main`, and epistemic review via
  `procedures/implementation-review.md`.

Concept introduced:

- In a feature implementation chain, the selected HCI specification should not
  drift implicitly from feature to feature.

Review questions:

1. Why should implementation ask for an HCI specification before code changes?
2. What remains stable across a chain of feature implementations?
3. Why can a slice span multiple prompts under the same HCI
   specification?

### Instruction precision and rename

Completed slice:

- Added precision and plain-language guidance to `procedures/strategic-concept-design.md`,
  `procedures/operational-slice.md`, and `procedures/implementation.md`.
- Moved the instruction documents to `procedures/`.
- Renamed files to remove the old suffix from the instruction filenames.
- Updated repository references from old instruction paths to `procedures/...`.

Concept introduced:

- Current-state and target-state explanations should name actual evidence:
  files, commands, types, functions, behavior, or missing evidence.

Review questions:

1. What makes a current-state explanation ambiguous?
2. Why should chat explanations be shorter but more precise than written
   instructions?
3. What evidence should be named when describing a target state?
